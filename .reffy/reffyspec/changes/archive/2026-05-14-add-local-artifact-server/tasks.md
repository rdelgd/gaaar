## Tasks

- [x] Add a new `serve` subcommand to `index.mjs`.
- [x] Implement local static serving restricted to `reports/`.
- [x] Add markdown-to-HTML rendering for `.md` files.
- [x] Add a simple directory listing page for the served root.
- [x] Add guardrails against path traversal outside the configured root.
- [x] Document the command and examples in `README.md`.
- [x] Manually validate `gaaar serve`, directory listing, markdown rendering, non-markdown file serving, and invalid path rejection.
